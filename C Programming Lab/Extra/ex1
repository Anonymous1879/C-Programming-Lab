Write a program in c to find out whether the nth term of the Fibonacci series is a prime number or not' Read the value of n from the user and display the result in the main function- use separite user-defined functions ro generare r#';in il";;;;; i.rr" *0," check whether a nurnber is prime or not

#include<stdio.h>

int fibo(int);

int main()
{
	int n,nthterm,result;
	printf("Nth term");
	scanf("%",&n);
	nthterm=fibo(n);
	if prime(nthterm)==2
	printf("Prime");
	else
	printf("Not prime");
}

int fibo(int x)
{
	int i,a = 0, b = 1, c=0;
	for (i = 0; i <= n; ++i)
	{
	c=a+b;
	a=b;
	b=c;
	}
	return c;
}

int prime(int y)
{
	int i,count =0;
	for(i=0;i<y;i++)
	if(y%i==0)
	count++;
	return count;
}


take a number
generate fibo using usd
fibo result
result pass to prime
tae result prime
